{
  "name": "alae-app",
  "version": "1.0.0",
  "description": "",
  "main": "src/mapper.js",
  "scripts": {
    "mapper": "mkdir dist && node src/mapper.js",
    "template": "mkdir dist && mustache ./dist/result.json ./src/template.html ./dist/output.html",
    "build": "mkdir dist && node ./src/mapper.js && mustache ./dist/result.json ./src/template.html ./dist/output.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mustache": "^2.3.0"
  }
}
